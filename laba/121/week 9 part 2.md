![[Pasted image 20241204010458.png]]
1)Нам нужно спросить сколько{}{}{} засканить и отдать в чар арей 
2)Дальше пиздец нам нужно както понимать если у нас матч или нет для этого мы создаем производную матчед которая в базе всегда тру 
3)Смотрим нга каждую литеру пока не дойдем до конца арея \0
4)Если { то мы просто пушем 1 в писю
5)потом если } то мы сначало делаем result = pop() мы вызиваем поп и кидаем в резулт если резулт -1 значит что пусто значит ошибка и брейк останавливает именно внутреность иф
6)Потом мы проходим через все цифри все ок или не и дем в некст 
7)Если оно матчед и именно 1 и топ ноль тода тру 
matched        // This is either 1 (true) or 0 (false)
top == NULL    // This checks if top pointer is NULL (empty stack)
      if (matched == 1 && top == NULL)
тожк самое