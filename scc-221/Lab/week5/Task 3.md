![[Pasted image 20251115232107.png]]
![[Pasted image 20251115232331.png]]![[Pasted image 20251115232340.png]]Create table game (app_id INT, PRIMARY KEY(app_id)); Create table spy_stats (app_id INT, developer VARCHAR(20), FOREIGN KEY (app_id) REFERENCES game(app_id), PRIMARY KEY(developer), UNIQUE(app_id)); Create table Reviews (app_id INT, review_score INT, FOREIGN KEY (app_id) REFERENCES game(app_id), PRIMARY KEY(review_score), UNIQUE(app_id)); Create table Tags (app_id INT, tag VARCHAR(20), FOREIGN KEY (app_id) REFERENCES game(app_id), PRIMARY KEY(tag), UNIQUE(app_id)); Create table Promotional(app_id INT, header_image VARCHAR(200), FOREIGN KEY (app_id) REFERENCES game(app_id), PRIMARY KEY(header_image), UNIQUE(app_id)); Create table Categories (app_id INT, category VARCHAR(20), FOREIGN KEY (app_id) REFERENCES game(app_id), PRIMARY KEY(app_id,category)); Create table Genres (app_id INT, genre VARCHAR(20), FOREIGN KEY (app_id) REFERENCES game(app_id), PRIMARY KEY(app_id,genre));